import { ethers } from "ethers";

export const ETH_MARKET = ethers.utils.formatBytes32String("ETH");
export const BTC_MARKET = ethers.utils.formatBytes32String("BTC");

export const OTUS_FEE = 0.05; // 5% fee / YEAR
export const MAX_NUMBER = Infinity; // Max positive number
export const MIN_NUMBER = 0; // Max positive number
